const managerManaging = (theme) => ({
  paperCommon: {
    padding: theme.spacing(2),
  },

  inputTextAdd__textField: {
    [theme.breakpoints.down('xs')]: {
      width: '100%',
      marginBottom: theme.spacing(1),
    },
  },
})

export default managerManaging
