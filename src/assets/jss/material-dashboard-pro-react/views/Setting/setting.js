const setting = (theme) => ({
  paperCommon: {
    padding: theme.spacing(2),
  },

  setMarginBottom: {
    marginBottom: theme.spacing(2),
  },

  setPositionRelative: {
    position: 'relative',
  },
})

export default setting
